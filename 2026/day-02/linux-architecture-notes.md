kernel :- It is core part of Operating System ,its directly interact with hardware.

User space :- its is normal OS and its run user program or application

init/System :- after the kernel loades it will be start

How to create process and managed it
--> every runnable commands create a process.

-->if any process create then it will be managed like start,stop,control and kill

for example --> sudo systemctl start nginx

what systemd does and why its matter

--> systemd is init system , its starts after kernel loading and systemd start other services like network 

it is important because its manage and control everything after boot start.
